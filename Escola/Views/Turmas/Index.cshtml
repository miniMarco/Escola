@model IEnumerable<Modelo.Turma>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Descricao)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Descricao)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.TurmaId }) |
                @Html.ActionLink("Details", "Details", new { id=item.TurmaId }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.TurmaId }) |
                @Html.ActionLink("Adicionar Aluno", "AdicionarAluno", new { id = item.TurmaId })
            </td>
        </tr>
    }
    
    </table>
    @Html.ActionLink("Início", "Index", "Home")
</body>
</html>
